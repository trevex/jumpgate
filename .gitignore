# Superpowers workflow artifacts (specs/plans) — temporal, not project docs
docs/superpowers/

# direnv / nix
.direnv/
result
result-*
# generated symlink by git-hooks.nix on devshell entry
.pre-commit-config.yaml

# Rust
/target/
**/target/

# Go
/bin/
go.work.sum

# Node / web
node_modules/
/web/node_modules/
/web/dist/
/web/test-results/
/web/playwright-report/
/.devdata/
# web/src/gen is intentionally committed (generated TS stubs, like warden/gen)

# Env / secrets
.env
*.local

# Generated Go protobuf stubs ARE committed (warden/gen) so consumers
# build without a codegen step. Rust protobuf stubs are generated into target/
# by build.rs (already ignored above).
warden/warden-meshcert
warden/warden-bootstrap

# kind smoke/demo artifacts
/jumpgate
jumpgate-mesh-ca.pem

# e2e runtime fixtures (built CLI binary, exported mesh CA, per-actor config)
/test/fixtures/

# Local git worktrees for isolated task execution
.claude/worktrees/

# API/DB benchmark suite run artifacts
warden/internal/bench/bench-explain/
warden/internal/bench/bench-summary.md
